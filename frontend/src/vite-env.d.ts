/// <reference types="vite/client" />

/** Short git commit hash, injected at build time via Vite define */
declare const __APP_COMMIT__: string;

/** Build timestamp in YYYY-MM-DD HH:mm format, injected at build time via Vite define */
declare const __APP_BUILD_TIME__: string;

