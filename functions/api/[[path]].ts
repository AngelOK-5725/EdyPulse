/**
 * EduPulse API — Cloudflare Pages Functions (DEPRECATED).
 *
 * The frontend now communicates directly with the Render FastAPI backend.
 * All API requests from the frontend go to https://edypulse.onrender.com/api/*
 */
export function onRequest() {
  // Intentionally empty — frontend uses Render backend directly
}
